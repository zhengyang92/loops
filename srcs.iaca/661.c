#include "../iacaMarks.h"
#include <stdint.h>
#include <stdio.h>






int
fn (float total_width, float *blks)
{
  int blkcnt = 0;
  for (int i = 0; i < blkcnt; i++)
     { IACA_START
      total_width += blks[i];
} IACA_END }
