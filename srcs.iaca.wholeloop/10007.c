#include "../iacaMarks.h"
#include <stdint.h>
#include <stdio.h>






int
fn (const int *alphas, int min_a, int n)
{
  IACA_START for (n = 255; n > min_a && alphas[n] == 0; --n)
    {
     } IACA_END
}
