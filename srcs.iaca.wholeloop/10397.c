#include "../iacaMarks.h"
#include <stdint.h>
#include <stdio.h>






int
fn (int indexes[9], int i, float *board)
{
  int handicap = 0;
  int move_num = 0;
  IACA_START for (i = 0; i < handicap; ++i)
    {
      board[indexes[i]] = 1;
      ++move_num;
     } IACA_END
}
